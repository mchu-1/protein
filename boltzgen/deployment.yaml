# deployment.yaml
app_name: "boltzgen"

# Infrastructure Settings
gpu_type: "A100"      # Options: T4, L4, A10G, A100
timeout: 14400        # 4 hours (safeguard for large batches)

# Experiment Settings
config_file: "3di3-config.yaml"
script_file: "job.sh"

# Run Strategy (1K designs)
total_designs: 100
batch_size: 10       # 10 parallel GPUs running 10 designs each